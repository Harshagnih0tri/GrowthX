import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0F0F12);
  static const Color surface = Color(0xFF1A1A1F);
  static const Color surfaceElevated = Color(0xFF212127);
  static const Color surfaceBorder = Color(0xFF2A2A31);

  // Brand
  static const Color primary = Color(0xFF7C6CFF);
  static const Color primaryMuted = Color(0xFF4A3FCC);
  static const Color accent = Color(0xFF00E5C9);

  // Semantic
  static const Color success = Color(0xFF4ADE80);
  static const Color warning = Color(0xFFFBBF24);
  static const Color error = Color(0xFFF87171);
  static const Color info = Color(0xFF60A5FA);

  // Feature accents (used consistently per module across the app)
  static const Color study = Color(0xFF60A5FA);
  static const Color gym = Color(0xFFFB923C);
  static const Color water = Color(0xFF22D3EE);
  static const Color weight = Color(0xFFC084FC);
  static const Color journal = Color(0xFFFBBF24);
  static const Color habit = Color(0xFF4ADE80);

  // Text
  static const Color textPrimary = Color(0xFFF5F5F7);
  static const Color textSecondary = Color(0xFFA1A1AA);
  static const Color textTertiary = Color(0xFF6B6B75);

  // Utility
  static const Color divider = Color(0xFF27272E);
  static const Color shadow = Color(0x40000000);
}